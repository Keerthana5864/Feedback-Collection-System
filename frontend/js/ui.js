const UI = {
    // Toast functionality removed per user request for cleaner presentation look
    showToast(message, type = 'success') {
        console.log(`Notification (${type}): ${message}`);
    }
};
